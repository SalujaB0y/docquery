OPENAI_API_KEY=sk-...
SUPABASE_URL=https://your-project.supabase.co
SUPABASE_SERVICE_ROLE_KEY=eyJ...
PORT=3001
FRONTEND_URL=http://localhost:3000
RATE_LIMIT_MAX=200

# only needed to run `npm run eval` — a password you choose for the dedicated eval
# service account (backend/src/eval/authClient.ts), not tied to any real user
EVAL_SERVICE_PASSWORD=choose-a-real-secret-here
